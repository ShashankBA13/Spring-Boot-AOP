server:
  port: 9092

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aop?autoReconnect=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

logging:
  level:
    org.springframework.cache: DEBUG

dev-message: |
  Hello from application.properties
